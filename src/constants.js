// Maximum number of elements per graphic
export var MAX_ITEMS = 100;
// Test offset value
export var INDEX_OFFSET = 8350;
// Width of a tick with the date
export var TICK_WIDTH = 30;
// Pad padding size.
export var PADDING = 5;
// Debounce time to filter the quantity of elements in the graph.
export var DEBOUNCE = 100;
// Number of points per pixel allowed in width
export var POINTS_PER_WIDTH = 10;